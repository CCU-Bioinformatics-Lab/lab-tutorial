# CLAUDE.md — 這個專案的工作方式

給未來在這個 repo 工作的 agent。這些是實際做過一輪、**踩過才知道**的東西，
不是通則。專案結構與指令看 `README.md`；這份檔案講的是**怎麼做才不會做壞**。

---

## 0. 最快的檢查清單

```bash
node tools/build.mjs              # 建置（會擋掉未定義詞彙、寫死顏色的 SVG…）
node tools/verify.mjs             # file:// 安全 ＋ 教材自成一體
node tools/audit_order.mjs        # 教學順序稽核
python3 tools/check_svg_layout.py # SVG 文字重疊／超出畫布
```

**四支都要過。** 後兩支各抓出過 13 個我用眼睛看不出來的問題。

改完 `src/` **一定要重建並把 `site/` 一起 commit**。`site/` 不是可丟棄的建置產物，
它是學生用 `file://` 直接打開的交付物（見 §7）。忘記重建的話 repo 裡的 `site/`
就跟 `src/` 不一致，而且**沒有任何錯誤訊息** —— CI 會比對這兩者並擋下來。

push 到 `main` 之後 CI 會自動發布，不需要手動推 `gh-pages`。

**不想發布的模組**：在 `modules.json` 那一筆加 `"draft": true`。頁面照建（本機
`file://` 讀得到，CI 的 `site/`↔`src/` 比對也才不會失敗），但不出現在首頁、學習
路徑時數、`print-all`、上下篇 pager 與 glossary 的任何連結上；CI 在上傳 Pages
artifact 之前由 `tools/strip_drafts.mjs` 把檔案刪掉。拿掉這個欄位就會發布出去。

這是**未列出**，不是**私密** —— repo 是公開的，`src/` 與 `site/` 裡的原始檔照樣
讀得到。真的不能公開的東西就不要 commit 進這個 repo。

---

## 1. 教材必須自成一體（最重要的一條）

讀者手上**沒有**任何投影片、論文或簡報。內文出現下列任何一種就是 bug：

- Deck 代號（`Deck A`、`Deck B`…）
- 投影片編號（「投影片 27」、`slide 4`）
- 「五份投影片有一個共同的問題」這類**後設敘述**
- 用「這個 M7 會講」來**迴避當下該給的解釋**

`verify.mjs` 第 10 項會擋前三種（含 `quizzes.json`、`glossary.json`）。

### 前向指標 vs 前向迴避

指向後面模組**可以**，但必須是「預告深度」而不是「把答案推給別人」。

判準：**把那句話刪掉，當下的段落還講得通嗎？**

```
✗ 「這個缺口就是後面兩篇要補的洞。M7 會完整講一次。」   ← 答案在別處
✓ 「真的變異一定是從某一條既有 haplotype 長出來的 ——
     它會形成一條新路徑，而那條路徑跟來源只差這一個位置。
     這個想法叫 triplet graph，之後會用一張圖完整拆解。」  ← 答案在這裡
```

### 我第一輪是怎麼做錯的

我把每個模組的第五段設計成「這出現在哪」＋投影片引用卡。那是**架構層級**的錯，
所以修的時候得重寫 15 個章節。第五段現在是「動手做」——
真的指令、真的檢查步驟、真的排查順序。

順帶一提：Deck 代號其實對應具體工具，換掉之後**資訊量反而增加**。
不要寫「Deck B 的做法」，寫「longphase-to 的做法」。

### 這條規則禁止的是「源頭」，不是「延伸閱讀」

上面全部在講同一件事：**不要叫讀者去找我們用來寫這份教材的原始投影片／論文**，
因為讀者手上根本沒有那些檔案，那樣寫就是把當下該給的解釋推掉。

這跟「提到 DeepVariant、Clair3、ClairS、ClairS-TO、LongPhase、LongPhase-S、
LongPhase-TO 這些真實存在的軟體時，附上論文出處與 GitHub repo 連結」是兩件不同的事，
**後者鼓勵做**。差別在於方向：

```
✗ 禁止：「這個做法在 Deck A 投影片 27 有完整推導。」   ← 讀者拿不到 Deck A
✓ 鼓勵：「本章方法對應 LongPhase-S 的預印本（bioRxiv, 2025，
          doi 10.1101/2025.11.20.689492），程式碼在
          github.com/CCU-Bioinformatics-Lab/longphase-s。」 ← 讀者可以自己去查
```

判準跟前面一樣：**內文的解釋本身必須完整**，citation／連結是「想追下去可以去哪裡」，
不是「去那裡才看得懂」。具體做法：

- 在**第一次完整教這個工具**的模組**最末端**（`{{quiz:mXX}}` 之後、`practice`
  區塊的 `</section>` 之前 —— 也就是整頁最後的內容）加一段「原始文獻與程式碼」
  （`m05`、`m07`、`m11`、`m12`、`m13` 已經有這個 pattern，照抄格式即可），
  放論文出處（作者、標題、期刊、年份，有 DOI 就附 DOI）與 GitHub repo 的
  `<a href>` 超連結
- 不要在正文段落中間插入連結打斷解釋節奏 —— 集中放在頁尾的引用區塊，
  不要放在 `evidence`／`concept` 等中段區塊
- 純文字外部連結 `<a href="https://...">`（不含 `target`／`rel`，跟 `m00` 既有寫法一致）
  在 `file://` 下完全安全；`verify.mjs` 的第 5 項只擋會**載入資源**的外部參照
  （`src=`、`@import`、CSS `url()`），純連結不受影響
- `verify.mjs` 第 10 項的正則只抓「Deck 代號／投影片編號／看懂某論文」這類
  **後設用語**，正常的「作者、標題、期刊、年份」引用格式不會被擋

---

## 2. 視覺化的標準比你以為的高很多

### 先去看原始素材，不要憑空發明

我第一輪只讀 speaker notes 就開始畫圖。等到真的把 pptx 轉出來看，
才發現原始圖具體得多 —— 例如 soft clip 那張是把參考基因體畫成**一格一個字母的方框列**，
CIGAR 直接標在對應段落的正下方。我自己想不出這個表達方式。

```bash
soffice --headless --convert-to pdf --outdir <out> deck.pptx   # 約 30–60 秒
```

然後用 Read 工具讀那個 PDF（帶 `pages` 參數，一次最多 20 頁）。
**畫任何解釋性的圖之前，先看原始素材怎麼畫的。**

注意：投影片上的圖多半是 PowerPoint 原生形狀，不是嵌入的圖片 ——
所以 `ppt/media/` 裡找不到，一定要 render 才看得到。

### 密度目標

**每千個中文字約 2 個視覺元素。** 用這段量：

```python
ns,nw,nf = 各數 {{svg: / {{widget: / {{fig:
cjk = len(re.findall(r"[一-鿿]", 去掉標籤與 macro 的內文))
density = (ns+nw+nf)/(cjk/1000)     # < 1.6 就該補圖
```

### 什麼一定要畫圖

- 任何**流程**（A → B → C）
- 任何**對比**（真變異 vs 定序錯誤、有對照 vs 沒對照）
- 任何**「為什麼會這樣」的機制**（電流訊號為什麼數錯、為什麼兩個點不夠）
- 任何**數字推導**（VAF 的分子分母、precision 漲了 F1 卻沒漲）
- 條列超過三項而且項目之間有結構關係

**判準：如果你需要用兩段文字解釋一個機制，那就是一張圖。**

### 對比圖的關鍵設計

畫「A 與 B 不同」時，要讓**表面上看起來一樣的部分真的一樣**。
例如真變異與定序錯誤的對照圖，兩邊的支持 read **總數刻意設成幾乎相同** ——
因為重點正是「光看數量分不出來，要看分布」。如果一邊 12 條一邊 3 條，
讀者會學到錯的判準。

---

## 2.5 首頁是給「還沒決定要不要學」的人看的

首頁的工作**不是**展示課程內容，是讓一個零基礎的人願意點進第一個模組。

### 卡片描述寫「問題」，不要寫「術語清單」

實際發生過的反例：

```
✗ M8　purity、DNA fraction、ploidy、copy number、multiplicity、LOH、TINC
✗ M12　GHIR → LCVF → purity 迴歸 → purity 感知過濾 → 最終 somatic haplotagging
```

零基礎的人往下滑看到這些，**得到的訊息是「這我學不會」**。改成：

```
✓ M8　同樣看到一個數字，為什麼可能代表三種完全不同的情況？
✓ M12　有正常組織可以對照時，完整的腫瘤分析流程長什麼樣？
```

`modules.json` 因此有兩個欄位：`q` 給首頁卡片（問題形式、零術語），
`sub` 給模組內頁副標題（可以用術語，那裡有圖與互動撐著）。

### 開場要講「這個實驗室在做什麼」，不要從研究問題切進去

第一版開場是「某肺癌檢體有兩個變異，它們在不在同一條染色體拷貝上？」——
那是 M6 的核心問題，寫得也不算差，但它**預設了太多還沒教的東西**：
染色體有兩份、什麼是變異、什麼是相位、為什麼影響用藥。
零生物背景的資訊學生讀到這裡只會確認「我看不懂」。

改成一張三層圖 —— **輸入 → 軟體 → 輸出**：

```
① 定序機給你的東西    病人 → 取檢體（癌細胞與正常細胞混在一起）→ 定序 → 一疊長得一樣的字串
② 這個實驗室開發的軟體  C++
③ 軟體要算出來的東西    每條片段來自哪一條染色體 / 哪些位置是癌細胞才有的 / 癌細胞佔多少
```

**「輸入 → 處理 → 輸出」是資訊背景的人立刻看得懂的形狀。**
文字壓到兩句，重量全放在圖上。

### 具體事物比抽象編號好懂

「第 1 份 DNA / 第 2 份 DNA」→ 改成 **「父系 / 母系」**，而且染色體要**畫成一對**。
這是使用者的意見，而且他是對的：人本來就是一條來自父親、一條來自母親。

但**同一張圖裡必須寫出軟體做不到的事**：

> 軟體分得出這是兩條不同的染色體，但要知道哪一條來自父親，得另外定序父母。

否則讀者會以為 HP 標籤帶有親源資訊 —— 那正是 guardrail #1，
而原始投影片的口白就是這樣講錯的。
**「用直觀的說法 ＋ 當場標出它的界線」比「換一個不會錯但沒人懂的說法」好。**

### 自學者最先想問的四件事，首頁都要回答

給誰 · 需要先會什麼 · 要多久 · 讀完你能做什麼。
時數從 `est_min` 加總算出來，不要手寫（會過期）。

### 其他

- **一個明確的起點**。15 張卡片並列時，有人會從 M12 點進去然後放棄
- **首頁也要有圖**。一張學習路徑圖讓人一眼看到全貌與轉折點
- **技術細節收到最後**。`opaque origin` 這種詞不該出現在第一次打開的第二段，
  收進 `<details>` 或頁尾

---

## 3. 概念順序：tooltip 不能取代講解

一個術語有 glossary tooltip，**不代表它被教過了**。
在答案裡丟一個沒解釋的 PON 就是失敗，即使它有 tooltip。

`audit_order.mjs` 會列出：

1. 某個詞首次使用的模組**比講解它的模組更早**
2. 每個模組引入幾個新詞 —— **超過 14 個就偏多**

第 1 項不是每筆都要改：如果那個詞在使用處就有一句話定義（或有圖），可以接受。
要避免的是「丟術語＋叫讀者去別處找」。

### M0 的教訓（結構性問題）

第一輪 M0 引入了 **21 個新術語**，包含 PON、LOH、somatic haplotype ——
全都在很後面才教。根因不是粗心，是**結構**：
一個想當「完整導覽」的模組，會被迫把所有工具與概念都點名一次。

修法是讓 M0 只教它真正教的東西（降到 5 個詞），
用**一個現象**開場（同樣兩個突變、位置不同 → 用藥不同），
而不是用一趟詞彙巡禮開場。

---

## 4. CJK 排版跟拉丁排版是兩件事

| 事項 | 規則 |
|---|---|
| 行距 | 18px 中文需要 **≈26px** 行距。給 18–20px 就會擠在一起 |
| 字寬估算 | CJK 一個字 ≈ font-size；拉丁 ≈ 0.55 × font-size |
| 漢字與拉丁之間 | 沒有瀏覽器會自動插 1/4 em，要自己補 `margin-inline: .15em` |
| justify | **不要用**。CJK ＋ 拉丁混排會在英文詞周圍產生河流 |
| 粗體 | 內嵌英文 term 用**點線底線**，不要用粗體（系統 CJK 合成粗體很醜） |
| `lang` | `<html lang="zh-Hant-TW">` 必填 —— 瀏覽器據此挑 CJK fallback |

### 字型（我在這裡弄錯過）

**PingFang TC 存在**，但 `ls /System/Library/Fonts/` 看不到它 ——
macOS 把它註冊成 sealed system asset。正確的探測方式：

```bash
system_profiler SPFontsDataType | grep -i "family: PingFang"
```

我用 `ls` 判定它不存在，還把這個錯誤結論告訴了使用者。**用對的工具驗證。**

zh-Hant 的字型堆疊要**刻意排除** `Hiragino Sans GB` 與 `STHeiti` ——
它們是簡體字面，會把「骨 直 說 內 錄」渲染成大陸字形。

---

## 5. `file://` 是另一個平台

瀏覽器給每個本機檔案一個 **opaque origin**，所以下列全部失效：

| 失效 | 因應 |
|---|---|
| `fetch()` / `XMLHttpRequest` | 資料做成 `*.data.js` 掛在 `window` 上 |
| `<script type="module">` / ESM | 只用 classic script |
| `@font-face` 相對路徑 | 只用系統字型堆疊 |
| 外部 SVG `<use href="x.svg#id">` | 符號**逐頁 inline** |
| iframe 跨框腳本 | 不要一個模組一個 iframe |
| 絕對路徑 `/assets/…` | 一律相對路徑 |

**而這些在 `http://localhost` 全部正常。**
所以「我在 localhost 測過了」**不是證據**。驗收必須用真實的 `file://` 路徑。

這也是為什麼**不用 three.js**：現代版本是 ESM-only，在 `file://` 直接載不起來。
k=4 的超立方體只有 16 個頂點 32 條邊 —— 那是 40 行矩陣運算，不是 WebGL 問題。

### localStorage 的陷阱

`file://` 是單一 opaque origin，所以**該機器上所有本機 HTML 共用同一個 bucket**，
而且綁在瀏覽器 profile 而非教材檔案。後果：

- key 必須 namespace（`ccu.lrcg.tutorial.v1`）
- 兩個學生共用一個帳號會互相覆蓋
- Safari 私密瀏覽下 `setItem` 會丟 `QuotaExceededError` → 全部包 try/catch，降級成記憶體

**所以「匯出／匯入進度」是第一級功能，不是附加功能。**

---

## 6. 一定要寫 linter，而且要讓它擋 build

四支工具各自抓不同的東西。**沒有它們，這些問題全部會出貨。**

### Canary 測試你的 linter

一支從來不響的 linter 等於沒有。寫完立刻故意違規：

```bash
# 塞進 fetch()、ESM import、CDN <img>、絕對路徑、外部 <use>
node tools/verify.mjs; echo "exit=$?"    # 必須抓到全部並 exit 1
```

我做過這個測試，5/5 全中。而 `verify.mjs` 加上「自成一體」檢查後
**幾秒內就抓到一處我自己漏掉的**（M2 圖說裡的「三份說明」）。

### 資料檔也要涵蓋

我把 deck 引用從 `src/modules/` 清乾淨後，忘了 `quizzes.json` ——
那裡還有 **20 處**。linter 要一起掃 `src/data/*.json`。

### 把「不能丟掉的東西」也變成檢查項

改寫章節時，**最容易不小心刪掉的就是最有價值的那部分**。
實際發生過：一次 M0 改寫把 guardrail #7（一條 read 是一個分子，不是一個細胞）
整個弄掉了 —— 概念在內文還在，但那個具體的陷阱警告消失了，而且沒人會注意到。

所以 `verify.mjs` 第 11 項會檢查**十個 guardrail 全部就位**，缺一個就 exit 1。

這個原則可以推廣：**任何「這個一定要有」的東西，就寫成 linter 規則。**
靠記憶或 code review 守不住，尤其是在大量改寫的時候。

---

## 7. 架構：source 在 `src/`，產出在 `site/`

**決定性理由只有一個**：只有這樣才能讓 SVG 同時是

- **(a)** 可獨立開啟／預覽／編輯的 `.svg` 檔，而且
- **(b)** inline 進頁面讓 CSS variable 主題化生效

沒有 build 就只能二選一：好主題化但不可維護（貼死在 HTML 裡），
或可維護但不能主題化（`<img src="x.svg">` 看不到頁面的 CSS variable）。
30 張圖的專案，這個 trade 就是全部。

### inline SVG 一定要做 id namespacing

同一頁 30 張 inline SVG，`id` 撞名會讓 gradient / marker **靜默抓錯 def** ——
這是最難查的 bug。做法：收集該檔案自己定義的 id，全部加前綴，
並改寫 `url(#x)`、`href="#x"`、`aria-labelledby`。

同理，把 15 個模組串成 `print-all.html` 時也要**逐模組 namespace**，
否則 `part-why`、`為什麼重要` 這些 heading id 會撞 15 次。

---

## 8. SVG 慣例（30 張圖靠這些維持一致）

| 項目 | 規則 |
|---|---|
| viewBox | **一律 `0 0 1000 H`**，寬度永遠 1000 user unit |
| 顏色 | **只用 class，不寫死**。build 會拒絕 `_defs.svg` 以外的字面色碼 |
| 無障礙 | 每張都要 `<title>` ＋ `<desc>`。`<desc>` 寫「看得懂的人學到什麼」，不是描述形狀 |
| 字級 | 標題 30 / 標籤 22 / 註解 18 / 刻度 15 |
| 線寬 | 細 1.5 / 一般 2.5 / 強調 4 |

### 色相不撞規則（第 22 張圖時你會忘記）

色相依**標記形狀**分工，不依語意混用：

```
read 本體（長圓角矩形） → haplotype 色
鹼基字母（文字）        → base 色
read 上方小圓           → variant class（somatic / germline / artifact）
read 下方小點           → methylation（實心 / 空心）
```

**任何一張圖都不把兩種編碼放在同一種形狀上。**
違反這條，讀者會把「紅色 read」誤讀成「somatic read」。

### 繪製順序：高亮一定要畫在填色圖形「之後」

SVG 沒有 z-index，**完全依文件順序繪製**。
`.region` 系列是 10% 不透明度的底色，如果寫在 `.read` 之前，
會被 read 的 fill 整個蓋掉 —— **畫面上什麼都看不到，而且不會有任何錯誤訊息**。

正確順序是：**read 底 → 高亮欄 → 字母**。
10% 在螢幕上還是偏淡，再補一個 `class="box ghost"` 的虛線框畫在最上層才夠明確。

### 同一個東西在前後兩張圖出現時，x 座標要對齊

「輸入的 read」與「輸出的 read」是同一批，只是被重新排序與著色。
兩處的 x 起訖與每一欄的中心**刻意用同一組座標**，眼睛才對得起來。
順帶：輸入層用 `hp0`（虛線外框 ＝ 來源未知），輸出層才換成 `hp1`/`hp2` 實線 ——
**「來源是軟體算出來的」這件事因此不必用文字說明，看顏色就知道。**

### 冗餘編碼

artifact 與 unmethylated 除了色相，另外帶**圖樣／空心**通道 ——
因為灰階列印會把色相整個吃掉，而且要照顧色盲。
`--hp1` 藍 / `--hp2` 橘是刻意選的：對 deuteranopia 的分辨度遠高於紅↔綠。

---

## 9. 互動元件

契約固定：`init` / `render` / `reset` / `check` / `getState` / `setState`

- `init` 建一次 DOM
- `render` 是 model → DOM 的**純函式**，可重複呼叫
- `reset` **只清使用者輸入，不重抽題目**（否則「重來」變成「換一題」）
- `check` 回傳 `{ok, score, message}`，**不可改動 model**
- 每個 widget 用 try/catch **隔離**，一個壞掉不能拖垮整頁

### 用 seeded PRNG，不要 Math.random()

`TW.rng(cfg.seed)`，seed 從 widget id hash 出來。這樣「隨機」資料在
不同人、不同 session 都一樣 —— meeting 上可以說「看第 7 條 read」而大家看到同一條。

### 領域上等價的答案要判對

手動 phasing 練習裡，把 HP1/HP2 **整組對調也是正確的** ——
因為編號本來就是任意的。這不是寬鬆，那才是對的。
如果你的 `check()` 判它錯，你就教錯了。

---

## 10. 教學內容的品質規則

- **每個測驗選項都要有 `why`，包含正確選項。** 只說「錯了」教不到任何東西
- **問題與答案放在一起。** 不要把答案推到別的模組
- **把原始素材沒明說的假設補上。** 例如「合成不同純度的樣本」隱含假設
  來源腫瘤是 100% 純的 —— 這句話沒寫在任何投影片上，但它是所有結果的邊界條件
- **「不要搞混」警告框是相對於原始素材最大的增值**（本專案有 10 個，不要刪）
- **解釋機制，不要只給方法名稱。** 讀者要能重建推理，不是記住名詞

---

## 11. 幾個具體的技術坑

### 無頭測試 widget 的 DOM shim

```js
ctx.window = ctx;   // 瀏覽器裡 window 就是全域物件，所以 window.TW= 會產生裸的全域 TW
```

我第一版把 `window` 做成獨立物件，結果 9 個 widget 全部 `TW is not defined`。
另外 `innerHTML` setter 必須**真的建立子節點**，否則
`lab.querySelector('input')` 回傳 null，測試會誤報失敗。

### 不要動使用者的原始檔

計畫原本要把 pptx 搬進 `decks/` 並改成 ASCII 檔名。**不要。**
用 `src/data/decks.json` 做代號對照就好 —— 工具照樣能引用
含中文、空格甚至 `?` 的檔名。

### grep 的災難性回溯

```bash
# 不要這樣：兩側 .{0,50} ＋ alternation 會跑到 timeout
grep -o -E ".{0,50}(A|B|C).{0,45}" *.html
```
用 Python 的 `re.finditer` 加手動切片。

### 圖片管線

- **先 crop 再 resize**。110 MP 的圖整張縮到 1600px 只會得到糊掉的色帶
- 超寬的帶狀圖（27000×3600）必須 **tile 切片**，縮圖等於 0.21 px/原始 px，刻度標籤會全滅
- `Image.MAX_IMAGE_PIXELS = 200_000_000`（明確抬高，不要設 `None` 拿掉防護）
- IGV 截圖用 WebP **q92**（q82 會把文字反鋸齒抹糊）
- 逐張處理並 `close()` —— 110 MP RGBA 常駐約 440 MB

---

## 12. 使用者正在編輯的檔案，不要搶著改

這個專案的使用者會自己動手改內容（實際發生過：他重寫了 M0 與 `m00-dependency.svg`，
把整個框架從「術語巡禮」改成「關聯查詢」，並把互動元件的選項從
`cis / trans` 改成 `同一份 DNA / 分開在兩份 / 資訊不足` —— 加上第三個選項是更好的設計）。

遇到這種情況：

- **先看他改了什麼、為什麼**，再決定要不要動
- 發現問題（例如上面那個 guardrail 消失）時，**回報 ＋ 加檢查**，
  不要直接改他正在寫的檔案
- 他的方向通常是對的：往**更少術語、更具體、更貼近讀者當下能理解的層次**走

---

## 13. 這個專案的事實（會被重新推導錯的）

指令與欄位名稱**一律以原始碼為準**。工具的說明文件有幾處與程式碼不符：

| 文件寫 | 實際是 |
|---|---|
| `--sv-window` / `--sv-threshold` | `--svWindow` / `--svThreshold` |
| `modcall` 的 `--bam-file` | `--methylbamfile` |
| `HP:i:1`（兩個 repo 的文件都這樣寫） | 只有 `longphase-s haplotag` 是整數；`somatic_haplotag` 與整個 `longphase-to` 都是字串 `HP:Z:` |

權威是 `Phasing.cpp` / `SomaticHaplotag.cpp` 裡的 `longopts` 表。

兩個工具的六處系統性差異（新人最常出錯）已整理在 M13 的對照表裡，
不要在其他地方重複維護一份。

另外：`longphase-to` 的 README 有一段 PoN 下載範例寫成 `PATH="PoN"`，
那會**覆蓋掉 shell 的 PATH 環境變數**。教材裡已改成 `$BASE`。
