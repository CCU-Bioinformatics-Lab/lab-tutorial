# ── 絕對不可進版控：未發表的口試投影片（約 121 MB）─────────────────
# 教材本身是自成一體的，不需要這些原始檔就能建置與閱讀。
# 只有 tools/index_decks.py 與 tools/extract_media.py 會用到它們，
# 而那兩支是一次性的素材抽取工具。
*.pptx
*.ppt
*.key
decks/

# 抽圖流程的中間產物
tools/deck_index.json

# ── 系統與編輯器 ────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# ── 相依套件與建置產物 ──────────────────────────────────────────
node_modules/
dist/
.next/
next-env.d.ts

# ── 祕密 ────────────────────────────────────────────────────────
.env
.env.*
!.env.example
